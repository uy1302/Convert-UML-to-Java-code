# Java Code Generator

#1: First create UML class diagram in draw.io, then export to XML file and choose compressed
#2: Download the XML file, get it into folder Convert-UML-to-Java-code, change file type from .xml to .drawio
#3: Run gui.py and select the file then convert 
